# Use the LLVM linker (lld) instead of GNU ld — much faster linking, which is an
# otherwise-serial bottleneck at the end of every build. Applies to all profiles.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=lld"]
